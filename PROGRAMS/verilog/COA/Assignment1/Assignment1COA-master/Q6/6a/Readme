Periodically, both a and b send messages to each other. Each prints the received message along with the timestamp.

binput is the input given by b and boutput is the corresponding result

ainput is the input given by a and aoutput is the corresponding result

network.v is the main function file

t_network.v is the testbench

vvp is the output file

vcd is the gtkwave file
